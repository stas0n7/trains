<%= form_with(model: @seated_carriage, local: true) do |form| %>
  <%= render 'common/errors', resource: @seated_carriage %>

  <div class="field">
    <%= form.label :train, "Attached to the train" %>
    <%= form.collection_select :train_id, Train.all, :id, :number %>
  </div>

  <div class="field">
    <%= form.label "Number" %>
    <%= form.text_field :number %>
  </div>


  <div class="field">
    <%= form.label "Seated seats" %>
    <%= form.text_field :seated_seats %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
